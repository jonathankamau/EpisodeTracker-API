version: '2.1'

services:
  test: 
    build:
      context: ../../
      dockerfile: docker/test/Dockerfile
    links:
      - database
    environment:
      DJANGO_SETTINGS_MODULE: settings
      DEV_DATABASE: postgresql://postgres:postgres@database:5432/test-episodetracker
    depends_on:
      database:
        condition: service_healthy
    command: /test.sh
    ports:
      - "8000:8000"   
    stdin_open: true
    tty: true   

  database:
    image: postgres
    restart: always
    expose:
      - "5432"
    environment:
        POSTGRES_USER: postgres
        POSTGRES_DB: test-episodetracker
        POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 10s
      retries: 5
